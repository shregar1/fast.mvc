"""
Optional dependency imports — delegated to ``fast_core``.

Prefer importing from the library in new code::

    from fast_core.utils import optional_import
"""

from fast_core.utils import optional_import

__all__ = ["optional_import"]
