# Optional FastMVC packages (fast_core + fast_db are in requirements.txt)
# Install only the ones you need, e.g.:
#   pip install -r requirements-optional.txt
#   or: pip install fast_dashboards fast_channels

fast_dashboards>=0.1.0
fast_channels>=0.1.0
fast_notifications>=0.1.0
fast_kafka>=0.1.0
fast_webrtc>=0.1.0
